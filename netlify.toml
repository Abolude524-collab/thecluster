[build]
  command = "npm run build"
  publish = "dist"

# Redirect all SPA client-side routes to index.html to prevent 404 errors on page refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
